#!/usr/bin/env bash
set -e
echo "Building production Tailwind CSS for ASENA Enterprise..."
cd "$(dirname "$0")/.."
npx -y tailwindcss@3.4.17 -i assets/css/tailwind.input.css -o assets/css/tailwind.output.css --minify
echo "Build complete: assets/css/tailwind.output.css"
